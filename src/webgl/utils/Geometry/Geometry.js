class Geometry{
  /**@type {Number[]} */
  positions = new Float32Array([])
  /**@type {Number[]} */
  normals = new Float32Array([])
  /**@type {Number[]} */
  uv = new Float32Array([])
  /**@type {Number[]} */
  indices = new Uint16Array([])
  constructor(){}
}

export default Geometry;
